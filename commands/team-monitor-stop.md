Stop the Team Monitor dashboard server.

Run the stop script to shut down the monitoring dashboard:

```
python "$CLAUDE_PLUGIN_ROOT/scripts/stop_server.py"
```

After stopping, confirm to the user that the Team Monitor dashboard has been shut down.
